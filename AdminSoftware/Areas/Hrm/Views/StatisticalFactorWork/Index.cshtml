@{
    ViewBag.Title = "Đánh giá thành tích lao động của nhân viên ";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<style>
    .k-radio:checked + .k-radio-label:after {
        top: 7px;
        margin-left: -1px;
    }

    .k-radio-label:before {
        top: 5px;
    }
</style>
<div class="panel panel-default">
    <div class="panel-heading">@ViewBag.Title</div>
    <div class="panel-body">
        <div class="row">
            <div class="col-md-12">
                <div class="box-action">
                    <div class="row padding-bottom-0">
                        <div class="col-md-8">
                            <div class="row">
                                <div class="col-md-3">
                                    <input type="text" id="MonthDate" class="form-control" value="@DateTime.Now.Month" style="height: 28px;" />
                                </div>
                                <div class="col-md-3">
                                    <input type="text" id="YearDate" class="form-control" value="@DateTime.Now.Year" style="height: 28px;" />
                                </div>
                                <div class="col-md-4">
                                    <button class="btn btn-primary btn-sm" id="btnSearchDate">
                                        <i class="fa fa-search"></i>
                                        Tìm kiếm
                                    </button>
                                    <button class="btn btn-primary btn-sm" id="btnPrint">
                                        <i class="fa fa-print"></i>
                                        In báo cáo
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-md-12">
                <input type="hidden" value="" class="form-control" id="UserId" />
                <div id="grdMain" event-dbclick="1"></div>
            </div>
        </div>
    </div>
</div>
@section scripts
{
    <script type="text/javascript">
        var department = @Html.Raw(Json.Encode(ViewBag.Departments));
        var statusWorkPlanDetail =  @Html.Raw(Json.Encode(ViewBag.WorkDetailStatusEnum));
        var statusWorkDetail = @Html.Raw(Json.Encode(ViewBag.StatusWorkDetail));
        var years = @Html.Raw(Json.Encode(ViewBag.Years));
        var months = @Html.Raw(Json.Encode(ViewBag.Months));
    </script>
    <script src="~/Areas/Hrm/JsAction/Kpi/StatisticalFactorWork/index.js"></script>
}
